# Hoist nothing surprising; keep the workspace deterministic.
auto-install-peers=true
strict-peer-dependencies=false
# NestJS + Prisma need some packages hoisted to the root node_modules to resolve.
shamefully-hoist=false
